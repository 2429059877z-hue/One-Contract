# One-Contract：原则型合同审查与起草 AI Skill

> 参赛说明文档
> 作者：马也
> 联系方式：15306823135 / zhangchi1@celg.cn

---

## 一、一句话定位

**One-Contract 是一套"原则型、修订优先、可验证"的中文合同审查与起草 AI Skill**——它把"资深律师怎么审合同"变成一套可执行、可追溯、可回归的工作流程：先理解交易目的和事实缺口，再按证据状态决定落笔方式，把能负责任落文的方案写成**真实 Word 修订**，把事实、授权和重大选择交给批注，最后用严格质量门验证交付文件。

它不堆叠规则命中，不输出一份风险报告了事——它交付**可直接签署、可谈判、可回滚、可验证**的合同修改。

---

## 二、要解决的问题

企业合同审查长期存在三个痛点：

1. **规则堆叠失效**：传统合同审查工具把律师经验写成大量 if-then 规则，触发条件越写越多、越写越死，强模型的实际推理空间被压缩，遇到真实交易场景就失灵；
2. **只报告不落笔**：多数工具识别风险后只输出风险清单，把后续起草工作退回给律师，无法真正发挥"修订优先"的价值；
3. **交付不可验证**：生成的 DOCX 常存在 OOXML 兼容问题，打开报错、修订痕迹混乱，执行器报告成功但最终文件不可用。

**One-Contract 的回答是**：用"原则型工作流"替代"规则堆叠"，用"真实修订"替代"风险报告"，用"质量门验证"替代"执行器自报成功"。

---

## 三、核心创新：七步工作流

One-Contract 的核心不是知识库有多大，而是这套**接近律师带教的工作方法**：

```
① 保全输入 → ② 证据账本 → ③ 宽识别推理 → ④ 形成方案
→ ⑤ 审查计划 → ⑥ DOCX 执行 → ⑦ 一致性复核 → 交付
```

### 第 1 步：保全输入，建立审查上下文
- 在副本上操作，保留原件及哈希；明确我方身份、交易目的、审查阶段、底线和议价能力；
- 企业合同先输出**稳定合同画像**（主类型/我方角色/场景/分类置信度），分类不明确时不自动路由。

### 第 2 步：建立事实与证据账本（核心创新）
每个关键事项标为四类**证据状态**——这是决定"怎么落笔"的开关：

| 证据状态 | 含义 | 默认动作 |
|---|---|---|
| `confirmed` | 材料足以支持 | **直接修订**（重大事项附简短批注）|
| `reasonable_assumption` | 可提主方案但需公开假设 | **修订＋批注**（说明假设/待确认/替代路径）|
| `major_choice` | 事实或授权改变结构方案 | **仅批注并升级**，用户选择后再修订 |
| `not_applicable` | 已排除 | 不输出 |

**不虚构事实**是硬边界：金额、日期、验收状态、权利放弃等未知事项一律用 `【待填：】` 占位或批注，绝不靠推测补齐。

### 第 3 步：宽识别并重新推理
- 从交易结构、文件体系、条款机制三层识别，**复杂事实决定动作强度，不决定是否分析**；
- 对每个实质问题回答六问（保护什么利益/什么事实改变判断/专业默认/推翻因素/条款联动/性质分类），结合具体交易重新推理，不做模板差异替换；
- **联动组必查**：付款-验收、解除-违约、保密-知识产权、限责-赔偿。

### 第 4 步：先形成正文方案，再选动作
- 优先问"能否在不虚构事实的前提下形成专业文本"；
- **正文只含确定主方案**，假设/建议/待核实一律进批注；能局部修改不整段重写。

### 第 5 步：形成审查计划（review plan）
每条 finding 含稳定 ID、条款位置、风险等级、证据状态、动作、目标文本、落文文本、假设、批注和法条依据。

### 第 6 步：应用并验证 DOCX（工程差异化）
- 执行器写入**真实 Word 修订**（`w:ins`/`w:del`）和批注；
- **质量门五重验证**：ZIP/XML 解析、编码与批注锚点、接受版+拒绝版、文本覆盖核对、LibreOffice 渲染逐页 PNG；
- **执行器返回 0 不等于完成**——任一硬门失败不得交付。

### 第 7 步：一致性复核与交付
- 核对原文/修订稿/批注/报告/日志一致性，失败项不得静默遗漏；
- 交付仅两份最终产物：`<原文件名>-修订版.docx` + `<原文件名>-审查报告.docx`；
- 高风险结论、对外发送前**保留律师终审**。

---

## 四、知识资产：原则卡体系

One-Contract 不堆规则，而是用**可追溯的知识资产**支撑判断：

```
知识资产：candidate → lawyer_approved → regression_passed → active → deprecated
运行时只加载 active 资产，候选资料不得伪装成正式知识
```

- **原则卡**：只提供判断坐标、默认倾向、推翻因素、硬边界——不锁死答案；
- **条款模块**：只提供结构、前提、可接受变体（平衡/让步/强保护）——按交易重写；
- **触发器**：确定性结构风险的硬信号检测（如竞业期限超二年、经济补偿标准错误）；
- **经验卡**：脱敏的真实案例观察（如"格式合同单方违约显失公平""违约金过高被酌减"）。

**通用条款原则与专项资产分层**：跨类型通用条款（管辖、付款节奏、通知、变更）由通用原则统一处理；专项资产在硬边界内深化该类型审查。专项深化优先，但不得突破法律与动作边界。

### 已激活的 5 个合同类型

| 类型 | 领域 | 资产规模 |
|---|---|---|
| 股权转让（EC-01-001） | 公司治理 | 原则卡+8模块+触发器+经验卡+联动组 |
| 设备、原材料采购（EC-02-002） | 买卖采购 | 原则卡+5模块+触发器+经验卡 |
| 协商解除劳动关系（EC-06-004） | 劳动人事 | 原则卡+5模块+触发器+经验卡 |
| 员工保密（EC-06-002） | 劳动人事 | 原则卡+5模块+触发器+经验卡 |
| 竞业限制（EC-06-003） | 劳动人事 | 原则卡+5模块+触发器+经验卡 |

知识资产规模：92 条款模块、41 联动组、20 原则卡、19 触发器、27 经验卡、51 来源记录、20 DOCX 骨架。

---

## 五、验证成果：三批盲测

One-Contract 用**独立样本 × 双臂盲测**验证质量——审查代理不知道对比的是哪两个版本：

| 盲测批次 | 样本 | 结论 |
|---|---|---|
| 股权转让（3 样本）| 真实股权转让合同 | 原则型版本经多轮修正后整体优于基线，据此修正动作政策、意见书格式、表6判断后激活 |
| 设备采购（2 样本）| 萃饮机/IT耗材采购 | 候选版明显优于基线；修正阶梯式付款立场、管辖处理、调价细分后激活 |
| 劳动人事（6 样本）| 协商解除/保密/竞业 | 候选版整体胜出；修正经济补偿表述、劳动协议管辖后激活 |

**盲测驱动的迭代机制**：每轮盲评 → 定位缺陷（知识缺口/判断失误/动作分流/工程失败）→ 修根因 → 回归 → 复测，确保修正不是堆补丁而是跨样本泛化。

关键工程修复（真实用户反馈驱动）：
- 修 `select_active_assets` 参数文档（不传角色/场景导致模块被过滤）；
- 修 `legal_basis` 必填校验（报告判"缺依据"）；
- 修 `scripts/docx` 与 python-docx 包名冲突（结构性改名 `docx_engine`）；
- 交付收敛为两份命名规范的最终产物。

---

## 六、技术实现

- **纯 OOXML 直出**：不依赖 LibreOffice 模板，直接写 `w:ins`/`w:del`/`w:comment`，严格验证 ZIP/XML/RELS；
- **Python 脚本分工**：Agent 做专业判断，脚本做确定性文档操作（定位、修订、批注、质量门）；
- **法条核验**：所有法律引用经元典权威数据库检索确认现行有效（劳动合同法/民法典/人社部指引等）；
- **盲测工具链**：accept_revisions（处理含修订源文件）、extract_targets（精确提取定位文本）沉淀为可复用脚本。

---

## 七、使用场景与边界

**适合**：中文商业合同的常规审查/起草/交付格式统一；批量合同一轮风险扫描和修订底稿；律师复核前的结构化整理。

**边界**：不替代律师终审；不处理无事实背景的合同；不覆盖涉外法域/高监管行业专项；不自动完成线下审批/登记/授权核验。

---

## 八、未来规划

- 激活剩余 15 个首批合同类型（融资担保、建设工程、租赁、服务等）；
- 沉淀更多脱敏真实案例为经验卡；
- 强化法条时效性自动核验；
- 探索多法域（涉外）合同审查扩展。

---

## 九、项目信息

- 开源仓库：https://github.com/2429059877z-hue/One-Contract
- 许可证：CC BY-NC 4.0（商用授权联系作者）
- 联系方式：15306823135 / zhangchi1@celg.cn
